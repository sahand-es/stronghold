package model.SiegeUtil;

import model.map.Block;

public class SiegeTower extends SiegeUtil{

    public SiegeTower(String name, int hp, Block block, int defencePoint , int speed) {
        super(name, hp, block, defencePoint,speed);
    }
}
